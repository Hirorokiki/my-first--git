文字列判定プログラム
概要
入力された文字列が、特定のルールに合致するかを判定するプログラムです。
具体的には、同じ文字の連続グループを作り、そのグループサイズの出現回数がすべて2回になるかを判定します。

主な機能
文字列判定機能

入力文字列を構成する各文字の連続グループのサイズを数え、そのサイズの出現回数がすべて「2」である場合に「はい」と判定します。
詳細解析モード

判定結果が「いいえ」だった場合に、その理由を詳しく表示する機能です。